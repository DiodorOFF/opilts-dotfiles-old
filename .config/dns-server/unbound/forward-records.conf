server:
    # Forward zone declarations for specific domains
    # You can add specific domains to forward to specific DNS servers
    
    # Example: Forward all Google domains to Google DNS
    # forward-zone:
    #     name: "google.com"
    #     forward-addr: 8.8.8.8
    #     forward-addr: 8.8.4.4
